<!--pages/user/orderlist/orderlist.wxml-->
<view class="wrap">
  <view class="page-title">
    <text class="title">订单</text>
  </view>
  <scroll-view class="content" bindscrolltolower="lower" style="height: {{height}}rpx;" scroll-y='true' lower-threshold='10'>
    <view class="user-order-list">
      <view class="order-item" wx:for="{{hisOrderList}}" wx:key="{{index}}">
        <view class="order-num">    
          <view class="title-num">
            <text class="item-num">订单编号：{{item.member_order_no}}</text>
            <text class="item-time">{{item.member_order_create_date}}</text>
          </view>
        </view>

        <view class="order-con">
          <view class='orderlist' wx:for='{{item.order_product_list}}' wx:key='{{index}}'>
            <text class="orderlist-title">{{item.product_name}}</text>
            <view class='price-num'>
            <text class="orderlist-counter">X{{item.quantity}}</text>
            <text class="iorderlist-price">￥{{item.price}}</text>
            </view>
          </view>

        </view>
        <view class="state-total">
          <view class="item-state">
            <text>{{item.member_order_status_info.member_order_status_name}}</text>
          </view>
          <view class="item-total">
            <text>共{{item.orderTotalNum}}份，合计<text class="price-total">￥{{item.total}}</text></text>
          </view>
        </view>
      </view>

    </view>
    <view class='more' wx:if="{{hisOrderList}}">
      <text>加载更多</text>
    </view>
  </scroll-view>


</view>